# ===== 大型資料（超過 GitHub 限制 / 不適合進 repo）=====
# 開頭加 / = 只排除「專案根目錄」的 data 與 runs，
# 不會影響套件內的 ultralytics_yolo11/data（Python 程式碼）
/data/
*.zip

# 模型權重（73~78MB，建議改放 GitHub Releases 或 Git LFS）
*.pt

# ===== 環境 / 快取 =====
.venv/
__pycache__/
*.pyc
*.cache
*.cache.npy

# ===== 其他 =====
.vscode/
mlflow/
*.pptx
*.md

# docs/ 內的說明文件要進 repo（排除上面的 *.md 規則）
!docs/*.md